CREATE TABLE IF NOT EXISTS mysqldb.properties (
    id BIGINT NOT NULL,
    application VARCHAR(255),
    profile VARCHAR(255),
    label VARCHAR(255),
    prop_key VARCHAR(255),
    value TEXT,
    channel_name VARCHAR(250),
    last_modified_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_on TIMESTAMP,
    PRIMARY KEY (id)
);